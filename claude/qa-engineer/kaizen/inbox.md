# Kaizen — Learnings Inbox: qa-engineer

> Append-only capture of durable, non-obvious environment facts the `qa-engineer` agent
> discovers during runs — raw observations, not conclusions. The maintainer (cobb)
> periodically distills this inbox (agent-maintenance skill §5): verifies each entry,
> routes it (prompt / knowledge base / project docs / discard), logs the promotion in
> `history.md`, and clears it. The agent only appends here; it never promotes.
>
> Entry format (append at the end):
>
> ```markdown
> ## YYYY-MM-DD — <the fact, one line>
> - **Evidence:** what was run/read/observed (command, file:line, output)
> - **Context:** the task where it surfaced, one line
> - **Suggested home:** prompt | knowledge base | project docs | unsure
> ```

## 2026-07-21 — falkor-chat: the M3 workflow executor is wired only when `FALKORCHAT_ENABLE_AGENT` is truthy; `FALKORCHAT_WORKFLOW_ENABLED=1` alone gives a 503
- **Evidence:** `server/falkorchat/app.py` `_build_default_app()` returns `create_app(services)` early on `if not config.ENABLE_AGENT:` — the `if config.WORKFLOW_ENABLED:` block that builds the executor/trigger and calls `services.set_executor(...)` is **nested inside** the agent branch. Without an executor, `services._require_executor()` raises `WorkflowEngineDisabledError` → **503** on `POST /workflow-runs`. Confirmed by running the app with both flags on (process-flow REST worked) — the LLM-free `access-request@v1` flow needs no model but still needs the agent flag set.
- **Context:** K-025 M3 acceptance pass — planning an LLM-free black-box run of the `kind:'process'` proof flow.
- **Suggested home:** project docs (`falkor-chat/AGENTS.md` M1-server section / `server/.env.example`) — the two flags read as independent and are not.

## 2026-07-21 — falkor-chat: `pytest -m live` does NOT wipe the global `reference` graph, unlike the default offline `pytest`
- **Evidence:** AGENTS.md warns that a `server` pytest run leaves `reference` cleared by the `wf_repo` fixture. After `./scripts/test_queries.sh` + default `pytest`, `seed_workflows.sh acme` printed `(created)` for both `reference` defs — wiped, as documented. After `pytest -m live` (which deselects all 533 offline tests, so `wf_repo` never runs) `reference` still held all four defs, and the live test seeds its own throwaway `ws:live`. So the re-seed obligation attaches to the *default* run, not to a live-only run.
- **Context:** K-025 M3 acceptance pass — sequencing suite runs against the documented `reference`-wipe trap.
- **Suggested home:** project docs (`falkor-chat/AGENTS.md`, the `seed_workflows.sh` scripts-table row, which currently says "after a `server` pytest run" without the marker distinction).


## 2026-07-25 — `redis-cli GRAPH.QUERY … --no-raw` returns a FLAT one-scalar-per-line stream, and filtering blank lines silently corrupts null/empty cells
- **Evidence:** against FalkorDB v4.18.11, `redis-cli -p 6379 GRAPH.QUERY cpg_falkorchat '<3-col query>' --no-raw` produced exactly `3 header names + (21 rows × 3 cells) + 2 trailing lines` = 68 lines — **no** `1) 1)` RESP nesting, no per-row grouping. Parsing recipe that worked: drop the first N lines (N = column count), drop the last 2 (`Cached execution:` / `Query internal execution time:`), regroup the remainder into N-tuples. **The trap:** a `null` cell and an empty-string cell both render as an *empty line*, so a `[l for l in out if l != ""]` filter (the obvious first attempt) drops cells and silently shifts every subsequent row into the wrong column — my first harness reported false divergences for `null`/`""` until I stopped filtering. Also: `redis-cli` renders `null` **indistinguishably from `''`**, which is precisely why the `cpg` MCP tool renders `None` as the literal `null`.
- **Context:** S9 live acceptance of the `cpg` MCP server — AC-3 required diffing tool output against the `redis-cli` fallback path, which needs a parser for both formats.
- **Suggested home:** project docs (`skills/cpg-analysis/SKILL.md` §1 fallback block, or `cpg/mcp/README.md` next to its "tools diffing this output against redis-cli" note)

## 2026-07-25 — FalkorDB accepts Cypher boolean literals case-insensitively (`False` ≡ `false`), so Python-style boolean rendering round-trips fine
- **Evidence:** `MATCH (m:METHOD) WHERE m.NAME='post_message' AND m.IS_EXTERNAL = false RETURN count(m)` → `2`; the same query with `= False` → also `2`, no error. Worth knowing because `skills/cpg-analysis/SKILL.md` gotcha #2 (*"Booleans are real booleans: `= false` not `'false'`"*) reads as if capitalisation matters — it does not; **quoting** is what breaks (`'false'` is a string). I nearly filed a Medium "broken round-trip" defect against the MCP tool's `True`/`False` rendering before testing the claim; it downgraded to cosmetic.
- **Context:** S9 acceptance — characterising tool-vs-`redis-cli` cell-rendering divergences by value type.
- **Suggested home:** project docs (`skills/cpg-analysis/SKILL.md` gotcha #2 — clarify that the hazard is quoting, not case)

## 2026-07-30 — FalkorDB's own `RESULTSET_SIZE` (default 10000) silently caps the `cpg` MCP tool's reported `rows=` total, contradicting the "always the true total" claim
- **Evidence:** `redis-cli -p 6379 GRAPH.CONFIG GET RESULTSET_SIZE` → `10000`. `cpg_falkorchat` has **110048** nodes (`MATCH (n) RETURN count(n)`), but `mcp__cpg__query(cpg_falkorchat, "MATCH (n) RETURN n")` reports `rows=10000` — not the true 110048 — with no indication this is itself a cap rather than an exact count. Confirmed the FalkorDB-level cap (not the tool's own `CPG_MCP_MAX_ROWS`) is what binds: raw `redis-cli GRAPH.RO_QUERY cpg_falkorchat 'MATCH (n) RETURN n.id LIMIT 50000'` also returns only ~10,000 rows despite the explicit `LIMIT 50000`. Below 10,000 true rows the tool's own accounting is exact (verified: `MATCH (m:METHOD) RETURN m.NAME` on 1968 methods reported `rows=1968` correctly while rendering only 200). So the tool's row-cap/char-cap machinery is honest; it's blind to a *second*, server-level cap sitting beneath it that neither `cpg/mcp/README.md` nor `docs/manuals/cpg-getting-started.md` mentions.
- **Context:** QA verification pass on `docs/manuals/cpg-getting-started.md` (TP-008) — testing the manual's "the underlying count is always the true one" claim about truncated results.
- **Suggested home:** project docs (`cpg/mcp/README.md`'s "Truncation is display-only" section, which currently states the `rows=` figure is always true without qualification — and `skills/cpg-analysis/SKILL.md` §2, as a 6th gotcha, since it silently returns a wrong-but-plausible number rather than an empty/null result).

## 2026-07-25 — In a `pysrc2cpg` CPG, `METHOD.CODE` holds only short signatures; the wide text lives on `LITERAL`/`BLOCK`/`CALL`
- **Evidence:** `MATCH (n) WHERE n.CODE IS NOT NULL RETURN labels(n)[1], max(size(n.CODE)), count(n) ORDER BY 2 DESC` on `cpg_falkorchat` → `LITERAL 4314 (12,223 nodes)` · `BLOCK 2715` · `CALL 2552` · `RETURN 958` · `METHOD` not in the top 8. Consequence for payload-size testing: the intuitive "wide projection" `MATCH (m:METHOD) RETURN m.CODE` yields only **1,951 chars** across all 1,968 methods, so a row cap binds long before any char/size cap — a test written that way exercises the wrong branch and passes vacuously. A genuine binder is `MATCH (n:LITERAL) WHERE size(n.CODE) > 400 RETURN size(n.CODE), n.CODE` (29,890 chars). Docstrings are `LITERAL` nodes, which is why they dominate.
- **Context:** S9 acceptance — plan §7.3 named the METHOD/CODE query as the char-cap probe; it does not bind the char cap, so the case was re-derived (filed as a test-design defect).
- **Suggested home:** knowledge base (`skills/joern-cpg/references/cpg-model.md` consumer-query facts — where `CODE` is actually wide)

## 2026-07-31 — falkor-chat: a workspace's `reference`-graph WorkflowDef going MISSING (per `verify_workflows.sh`) does NOT block `@mention`-triggered workflow execution
- **Evidence:** `./scripts/verify_workflows.sh acme` reported `FAIL` — `reference` MISSING for both `triage@v1`/`access-request@v1` while `ws:acme` snapshots were present — before this pass began (residual drift from an earlier `test_queries.sh` teardown per `AGENTS.md`'s documented hazard). Read `server/falkorchat/services.py::start_workflow_run`'s own docstring ("Start a run for a materialized def snapshot") and confirmed only `get_workflow_def_structure`/`diff_def_snapshot` (observability/diff endpoints) touch `db.reference_graph`; the trigger/execute path never does. Then live-confirmed: 3 independent `@mention` triggers all started and drove `triage@v1` runs successfully against `ws:acme` with `reference` still MISSING throughout. So `verify_workflows.sh FAIL` is not automatically a blocker for a live acceptance pass — check which code path actually reads `reference` before treating the mismatch as gating.
- **Context:** K-039 acceptance pass (`docs/test-plans/mention-reply-delivery.md` TP-01) — almost treated the FAIL as an environment blocker before tracing the actual read path.
- **Suggested home:** project docs (`falkor-chat/AGENTS.md`'s `verify_workflows.sh` scripts-table row, or `docs/DESIGN.md` §11) — currently only documents the FAIL/DIVERGE distinction, not which code paths are actually affected by a `reference`-side MISSING/DIVERGE.

## 2026-07-31 — falkor-chat: a `WorkflowRun` parked `status:"waiting"` (`waitsForHuman`) resumes on the *next message posted to its thread*, whether or not that message `@mention`s the assistant
- **Evidence:** Posted a plain, non-mention message into a thread that already had an open `waiting` run (parked after `intake`, per `triage@v1`'s `waitsForHuman` start node). The plain message silently resumed that run — graph query confirmed a second `intake` `StepRun`, timestamped to the plain message, with its own `PRODUCED` message — rather than leaving the thread workflow-free. Only a **fresh thread with no open run** correctly exercises "an ordinary message never triggers a workflow."
- **Context:** K-039 acceptance pass, TP-07 (regression check for the non-workflow message path) — the first attempt was accidentally confounded by reusing a thread from an earlier test item in the same pass; had to redo in a clean thread to get a valid result.
- **Suggested home:** project docs (`falkor-chat/docs/DESIGN.md` §6, or `trigger.py`'s "one handler per message" docstring) — worth one explicit sentence that resume is not mention-gated, since it's a genuine testability trap for anyone black-box-testing thread/message behavior with a leftover open run in scope.

## 2026-08-01 — falkor-chat: `POST /workflow-runs/{id}/input`'s own response body does NOT carry the `error` reason when that submission is what causes the run to fail — only a follow-up `GET /workflow-runs/{id}` does
- **Evidence:** Started a run with `maxSteps:1`, submitted valid input that drove it over budget. The `/input` call's response came back `{"runId":...,"status":"failed","ctx":{"request":{"role":"employee"}}}` — no `error` key anywhere in that body. A subsequent `GET /workflow-runs/{id}` on the same run showed `"ctx":"{\"error\":\"step budget exceeded\",...}"` — the reason only appears there. So a caller that inspects only the `/input` response on a fault (reasonable, since the docstring calls this "synchronous" and D-G promises the failed envelope comes back inline) will see `status:"failed"` with no explanation and has to know to re-`GET`.
- **Context:** `docs/manuals/workflows.md` verification (TP-03) — checking the manual's "if the run has failed, the reason it stopped [is shown]" claim against the raw API (the manual scopes that claim to the web UI panel, so this wasn't filed as a manual defect, but it's a real asymmetry in the API worth a future QA pass or `analyst` review knowing about).
- **Suggested home:** project docs (`server/falkorchat/api.py`'s `§12` comment block, which already documents the error map for this route) — one line noting the `error` key lands in the run's `ctx` on the *next* read, not in the triggering call's own response.

## 2026-07-31 — This WSL2 box has no native browser-automation stack (no playwright/selenium/puppeteer in the WSL Python/Node), but Windows Chrome + raw CDP over the mirrored-network localhost works as a fallback for visual acceptance checks
- **Evidence:** `python3 -c "import playwright"` / `import selenium` both `ModuleNotFoundError`; `node`/`npx` resolve only to the Windows install (`/mnt/c/Program Files/nodejs/node.exe`), no Linux-side Node. Working path found: launch Windows Chrome headless with remote debugging (`"/mnt/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu --remote-debugging-port=9333 --user-data-dir="C:\Windows\Temp\<profile>" <url>`), then `curl http://localhost:9333/json` from WSL to enumerate targets (mirrored networking makes the port visible cross-side, consistent with the existing LM-Studio finding), then drive it with a small Node script run via the *Windows* `node.exe` (not WSL Node, which doesn't exist) that opens the page's `webSocketDebuggerUrl` with Node's native `WebSocket`, sends `Runtime.evaluate`/`Page.captureScreenshot` CDP commands, and writes output to a `C:\Windows\Temp\...` path (a WSL-side path like `/tmp/...` passed as a `fs.writeFileSync` argument to the Windows node binary does **not** resolve — it has to be a native Windows path, then `cp /mnt/c/Windows/Temp/<file> <wsl-path>` to pull it back). This let a genuinely interactive check (click a toggle button, then screenshot the revealed panel) succeed where a plain `chrome.exe --headless --screenshot=...` (no click) would only have captured the initial, un-toggled page state.
- **Context:** K-039 item 3 acceptance pass — verifying the K-036 readiness banner's new `postSuccess` line and its degraded-state CSS class actually render in the running web UI (`falkor-chat/web/app.js` `renderReadinessPanel`), which required opening a click-gated panel, not just loading the page.
- **Suggested home:** knowledge base / agent context — worth a short note either in `qa-engineer`'s own context or wherever this lab documents its WSL↔Windows interop facts (the existing `severino-wsl-lmstudio` memory covers the LM-Studio-reachability case but not browser-automation tooling), since any future black-box UI check in this environment will hit the same missing-Linux-browser-stack wall.

## 2026-08-01 — `kiro-cli doctor` is not read-only: it auto-remediates shell integration by writing to `~/.bashrc`/`~/.profile`
- **Evidence:** Running `~/.local/bin/kiro-cli doctor` (intended as a pure status check, per the dispatching brief's "kiro-cli doctor shows Auth ✔") produced `✘ bash ~/.bashrc integration check ... Attempting to fix automatically... ✔ bash ~/.bashrc integration check` and the same for `~/.profile`. `tail`/`stat` confirmed both files gained a new "Kiro CLI post block" sourcing line, with mtimes matching the `doctor` run. The appends are idempotent and non-destructive (guarded by an `if [[ -f ... ]]` source line), but this is still an unannounced environment mutation outside any repo, from a command whose name and prior framing ("shows Auth ✔") suggested it only reports status.
- **Context:** `kiro-demo-agent` live acceptance QA — ran `doctor` during environment probing before test execution began, expecting a pure health check.
- **Suggested home:** project docs (`kiro/docs/plans/kiro-demo-agent.md` §2.3's `kiro-cli doctor` bullet, or a new `kiro/README.md` caveat) — worth one line warning that `doctor` is not side-effect-free, so a future agent doesn't run it reflexively expecting a status-only check.

## 2026-08-01 — falkor-chat: MCP `send_message` never schedules the `assistant` responder/workflow trigger — only the REST `POST /threads/{id}/messages` route does
- **Evidence:** Posted a mention-bearing message via a real MCP client (`kiro-cli`) and via direct code reading: `falkor-chat/server/falkorchat/api.py:143-168`'s REST handler is the only place `background.add_task(_safe_run_workflow, ...)` / `background.add_task(_safe_respond, ...)` is scheduled (it takes a `BackgroundTasks` FastAPI dependency); `falkor-chat/server/falkorchat/mcp.py`'s `send_message` tool (and `Services.post_message()` itself) has zero references to `BackgroundTasks`/`trigger`/`responder` in the whole file. Live-confirmed: 45s+ wait, healthy LM Studio, zero new message, zero new `WorkflowRun`. Every prior QA pass that validated "`@mention` triggers a reply" (`mention-reply-delivery-report.md`) posted via REST, never via a real MCP client — so this gap had never actually been exercised before.
- **Context:** `kiro-demo-agent` live acceptance QA (AC-2/TP-003) — filed as Defect D-1 in `kiro/docs/test-reports/kiro-demo-agent-report.md`.
- **Suggested home:** project docs (`falkor-chat/docs/DESIGN.md` §15, which frames MCP as "the agent front door" without noting this asymmetry) and `falkor-chat/docs/BACKLOG.md` (as a new K-item once filed) — any future black-box check of "does `@mention` produce a reply" must specify which front door (REST vs. MCP) it drove through; they are not equivalent today.

## 2026-08-01 — `tmux` (not `expect`, not raw stdin piping) is the reliable way to drive `kiro-cli`'s genuinely interactive TUI for black-box QA
- **Evidence:** Piping input via `printf '/tools\n' | kiro-cli chat ...` did not produce the expected slash-command output — the TUI appears to need real TTY/raw-mode behavior that a plain pipe doesn't provide (output showed only the startup banner, no command response). Switching to `tmux new-session -d ... ; tmux send-keys ... ; tmux capture-pane -p` worked cleanly for both a slash command (`/tools`) and literal `@`-containing free text, and reliably captured the rendered pane content for evidence. `expect` was not available on this box; `tmux`/`script` were.
- **Context:** `kiro-demo-agent` live acceptance QA (TP-002, TP-006) — needed to test the true interactive path (slash commands, literal `@` typing) that `--no-interactive` deliberately sidesteps.
- **Suggested home:** knowledge base (`qa-engineer`'s own context, or wherever this lab keeps interactive-CLI-testing technique notes) — a reusable pattern for any future QA pass against an interactive TUI tool in this environment.
