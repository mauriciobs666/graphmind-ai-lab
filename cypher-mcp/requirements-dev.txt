# Development dependencies — runtime deps plus the test runner.
# `setup.sh` installs this file, so a dev venv always carries the runtime set too.
#
# There is no "extras" concept in a requirements file; this second file is the
# equivalent of falkor-chat/server/pyproject.toml's `[project.optional-dependencies] dev`.
# cypher-mcp is a script run by path, not an installable package, so it stays on
# plain requirements files rather than a pyproject.
-r requirements.txt
pytest>=9.1,<10
